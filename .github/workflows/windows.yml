name: Windows

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

env:
  # Path to the solution file relative to the root of the project.
  SOLUTION_FILE_PATH: .
  BUILD_CONFIGURATION: Release


permissions:
  contents: read

jobs:
  build:
    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v3
    - name: Add Deps_Qt
      run: |
        git clone http://github.com/GenshinTianLiBattle/deps_qt.git
        cd deps_qt
        .\unzip.bat
        .\addpath.bat 
        cd ..
        copy deps_qt/QtMsBuild.7z GenshinTianLiBattleClient/
        copy deps_qt/7z.exe GenshinTianLiBattleClient/
        copy deps_qt/7z.dll GenshinTianLiBattleClient/
        cd GenshinTianLiBattleClient
        echo ${{Qt_Dir_Static}}
        7z.exe x QtMsBuild.7z
        set Qt_Dir_Static=D:\a\GenshinTianLiBattleClient\GenshinTianLiBattleClient\deps_qt\5.15.7\bin\
    - name: Add MSBuild to PATH
      uses: microsoft/setup-msbuild@v1.0.2

    - name: Build
      working-directory: ${{env.GITHUB_WORKSPACE}}
      # Add additional options to the MSBuild command line here (like platform or verbosity level).
      # See https://docs.microsoft.com/visualstudio/msbuild/msbuild-command-line-reference
      run: msbuild /m /p:Configuration=${{env.BUILD_CONFIGURATION}} ${{env.SOLUTION_FILE_PATH}}
